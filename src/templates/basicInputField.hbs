<!--suppress HtmlFormInputWithoutLabel -->
<input type="text" id="{{id}}" name="{{id}}" alias="{{alias}}" fieldName="{{fieldName}}"
  {{#if disabled}}disabled readonly{{/if}} {{#if value}}value="{{value}}"{{/if}}>